{
  "name": "streaming-service-root",
  "version": "0.1.0",
  "private": true,
  "description": "V-DEBUT HUB Streaming Service Root Workspace",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build --prefix frontend",
    "deploy:backend": "npm run deploy:live --prefix backend",
    "deploy:live": "npm run deploy:live --prefix backend",
    "deploy": "npm run build --prefix frontend && npm run deploy:live --prefix backend"
  }
}
